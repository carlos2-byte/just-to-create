import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carlos.calculadora_de_rescisao_clt_brasil',
  appName: 'Cálculo de Rescisão Trabalhista',
  webDir: 'dist'
};

export default config;
